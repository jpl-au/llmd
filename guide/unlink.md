# llmd unlink

Remove a link between two documents.

## Usage

```
llmd unlink <from> <to>
```

## Examples

```bash
# Remove a link
llmd unlink notes/meeting notes/action-items
```

## Notes

- Both the source and target paths must be specified.
- See `llmd guide link` for creating and listing links.
